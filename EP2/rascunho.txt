class TS {
    V0 * vo;
    ABB * abb;
    TREAP * treap;
    A23 * a23;
    ARN * arn;
    int n_comp_busca;
    int n_comp_insercao;
    int n_trocas;
    int n_rotacoes;

    void add(Key key, Item val, int est);

}

void TS::add(Key key, Item val, int est) {
    if(est == 1) {
        vo()
    }
}